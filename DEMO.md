# Easy Agent Demo

这是一个随手创建的测试文件，用来验证文件写入功能。

## 随便写点内容

- 项目: easy-agent
- 目的: 演示用
- 心情: 🌤️ 不错

## 一些代码

```ts
function hello(name: string): string {
  return `Hello, ${name}!`;
}

console.log(hello('Easy Agent'));
```

## 结束

就这样，没啥特别的。
